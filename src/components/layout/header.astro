---
import { ThemeButton } from '@components/theme/theme-button';
import { SITE_TITLE } from '@constants';

import HeaderLink from './header-link.astro';

const isDev = import.meta.env.DEV;
---

<header class="m-0 px-4 shadow-[0_2px_8px_rgba(var(--black),0.05)]">
  <nav class="flex items-center justify-between">
    <h2 class="m-0 text-base">
      <a href="/" class="no-underline">{SITE_TITLE}</a>
    </h2>
    <div class="internal-links">
      <HeaderLink href="/">Home</HeaderLink>
      <HeaderLink href="/blog">Blog</HeaderLink>
      <HeaderLink href="/posts">Posts</HeaderLink>
      <HeaderLink href="/projects">Work</HeaderLink>
      <HeaderLink href="/about">About</HeaderLink>
      {isDev && <HeaderLink href="/admin">Admin</HeaderLink>}
    </div>
    <div class="social-links hidden md:flex gap-2">
      <!-- <Bluesky />
      <Github />
      <LinkedIn />
      <Twitter /> -->
      <ThemeButton client:only="react">
        <div slot="fallback" class="h-7 w-7"></div>
      </ThemeButton>
    </div>
  </nav>
</header>
